# Git Commit Skill

Analyzes git changes (staged and unstaged) to craft an appropriate commit message and execute git commit.

## Usage

This skill is designed for AI coding agents implementing the [Agent Skills standard](https://agentskills.io/). 
It provides specialized workflows and instructions for analyzing git changes, drafting conventional commit messages, and committing changes.

See `SKILL.md` for the skill definition.
